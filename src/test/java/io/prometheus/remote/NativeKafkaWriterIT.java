package io.prometheus.remote;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativeKafkaWriterIT extends KafkaWriterTest {

    // Execute the same tests but in native mode.
}